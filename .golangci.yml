run:
  timeout: 5m
linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - gosimple
    - ineffassign
    - unused
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
  exclude-dirs:
    - migrations
